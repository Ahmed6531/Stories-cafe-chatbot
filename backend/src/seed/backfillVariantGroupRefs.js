// This script was used to backfill refId onto VariantGroup documents.
// refId has been removed — groupId is the single canonical identifier.
// Script is kept as a no-op placeholder in case it is referenced in package.json scripts.
console.log("backfillVariantGroupRefs: no-op — refId field has been removed.");
process.exit(0);
